%li.author{ class: "#{author.status}" }
  = link_to author_path(author) do
    = image_tag author.image, width: 40, height: 40, class: "avatar"
    = "#{author} (#{author.articles.count})"
    
  .shtick= author.shtick
