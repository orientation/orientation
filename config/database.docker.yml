default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  database: orientation
  username: <%= ENV['DATABASE_USERNAME'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>

development:
  <<: *default
  database: orientation_development

test:
  <<: *default
  database: orientation_test
